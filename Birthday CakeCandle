 int  count=0,max=INT_MIN;
        for(int i=0;i<candles.size();i++){
       if(candles[i]>max)
       
       max = candles[i];  
   }
   

   
   for(int i=0;i<candles.size();i++){
       if(max==candles[i])
       count++;
   }
    return count;
